
# scratch venv usado só para gerar/testar migrations localmente
.venv/
__pycache__/
*.pyc

# dados reais de seed (e-mail/papel, e futuramente listas de clientes/
# credenciais) nunca entram no repositório — só o script que os lê entra.
# Ver scripts/seed_usuarios.py.
scripts/*.local.csv
